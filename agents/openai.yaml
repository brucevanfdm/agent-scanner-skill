interface:
  display_name: "Agent Skill Scanner"
  short_description: "Security scan for Agent Skills (injection/exfiltration/tool abuse)"
  default_prompt: "Run a balanced security scan on this skill, list CRITICAL/HIGH findings first, then provide concrete remediation steps."
