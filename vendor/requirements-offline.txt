# Optional full dependency wheelhouse (for environments that need native YARA extras)
# Build with scripts/build-vendor-wheelhouse.sh on a connected machine.
PyYAML>=6.0.1
python-frontmatter>=1.0.0
tabulate>=0.9.0
yara-python>=4.5.4
click>=8.1.0
rich>=13.0.0
